[pytest]
# Sem isto, `pytest` na raiz coleta 0 testes (um CI ingênuo passaria "verde" sem
# rodar nada). testpaths aponta a descoberta para tests/, onde vive o conftest.
testpaths = tests
python_files = test_*.py
python_functions = test_*
